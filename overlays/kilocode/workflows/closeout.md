# closeout (Kilocode overlay)

---
description: Full CI closeout + local commit after dual APPROVED (never push)
---

Full CI per `{{COMPANION_ROOT}}/workflow/ci-ladder.md`; if Full = `n/a` require explicit user ack per `{{COMPANION_ROOT}}/rules/pre-commit-ci-gate.md`. Local commit only; never `git push`. Record the phase verdict in the roadmap file when one exists.
