# cursorEscape loop (always-on, thin)

Kilo Code host adapter for the owner's agentic loop. Full procedures load via companion **Read** — `{{COMPANION_ROOT}}/skills/`, `{{COMPANION_ROOT}}/workflow/`, `{{COMPANION_ROOT}}/agents/`. The `.kilocode/rules/` directory is auto-included always-on by the platform (backward compat); keep this composed rule thin — deep procedure is companion-resident.
