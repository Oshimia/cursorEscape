# closeout (Cline overlay)

After dual APPROVED:

1. Full CI per `{{COMPANION_ROOT}/workflow/ci-ladder.md` + repo scripts (never paired with reviewers).
2. If Full = `n/a`: explicit user acknowledgment per `{{COMPANION_ROOT}}/rules/pre-commit-ci-gate.md`.
3. Local commit only (pre-commit gate asserted); **never `git push`**.
4. Record phase verdict in the roadmap file (if a multi-phase plan) — `{{COMPANION_ROOT}}/skills/roadmap/SKILL.md`.
